
{
  "name": "pdf-generator-service",
  "version": "1.0.0",
  "description": "A microservice to generate PDFs from HTML using Playwright.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npx playwright install --with-deps chromium"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "playwright": "^1.45.1"
  }
}
